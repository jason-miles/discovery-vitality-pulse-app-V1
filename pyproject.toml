[project]
name = "vitality-pulse"
version = "1.0.0"
description = "Vitality Pulse — Shared-Value Analytics Portal (Databricks App)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "databricks-sdk>=0.30",
    "pydantic>=2.7",
]

[tool.uv]
package = false
